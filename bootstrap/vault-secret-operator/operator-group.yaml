apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: vault-operator
  namespace: devsecops-vault-secret-operator
spec:
  targetNamespaces: []  # Empty array means watch all namespaces